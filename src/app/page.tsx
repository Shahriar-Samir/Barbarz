import Banner from '@/components/Banner';
import Header from '@/components/Header';
import React from 'react';

const Home:React.FC = () => {
  return (
    <main>
      <Header/>
      <Banner/>
    </main>
  );
};

export default Home;